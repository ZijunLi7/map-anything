defaults:
  - default

# Number of views parameter for the multi-view datasets
num_views: 2

# Test Resolution
resolution_test_kitti360: ${dataset.resolution_options.512_1_33_ar}

# Test Set
# Sample 160 multi-view sets from each scene
# Kitti360: 8 scenes
test_dataset:
  "+ 180 @ ${dataset.kitti360_wai.test.dataset_str}"
