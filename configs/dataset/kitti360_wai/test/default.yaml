dataset_str:
  "kitti360WAI(
    resolution=${dataset.kitti360_wai.test.dataset_resolution},
    principal_point_centered=${dataset.kitti360_wai.test.principal_point_centered},
    seed=${dataset.kitti360_wai.test.seed},
    transform='${dataset.kitti360_wai.test.transform}',
    data_norm_type='${dataset.kitti360_wai.test.data_norm_type}',
    ROOT='${dataset.kitti360_wai.test.ROOT}',
    dataset_metadata_dir='${dataset.kitti360_wai.test.dataset_metadata_dir}',
    variable_num_views=${dataset.kitti360_wai.test.variable_num_views},
    num_views=${dataset.kitti360_wai.test.num_views},
    covisibility_thres=${dataset.kitti360_wai.test.covisibility_thres})"
dataset_resolution: ${dataset.resolution_test_kitti360}
principal_point_centered: ${dataset.principal_point_centered}
seed: 777
transform: 'imgnorm'
data_norm_type: ${model.data_norm_type}
ROOT: ${root_data_dir}/kitti360
dataset_metadata_dir: ${mapanything_dataset_metadata_dir}
variable_num_views: ${dataset.test.variable_num_views}
num_views: ${dataset.num_views}
covisibility_thres: 0.025
